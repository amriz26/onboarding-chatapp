/**
 * Re-export all schemas from the shared package for convenient import
 * within the web app. Components import from "@/lib/schemas" rather than
 * reaching into the shared package directly.
 */
export * from "@effect-mcp/shared"
